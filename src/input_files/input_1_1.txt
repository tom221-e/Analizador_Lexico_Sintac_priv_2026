INT: aux, id
BOOLEAN: es_mayor, si, A, B, D
INT: monto_total
FLOAT: promedio, c, valorDePrueba
ARRAY[10]: mediciones

PROGRAM

    mediciones[0] = 1.5
    mediciones[1] = 2.5
    mediciones[2] = 3.5
    mediciones[3] = 9.9

    print(mediciones[0])

    es_mayor = ! true
    id = -6
    valorDePrueba = -2.3
    % comentario antes del código

    {* comentario
    multilinea
    antes del programa *}

    si = ! false
    print (! ! si)

    % comentario entre sentencias

    mediciones[5] = 7.7

    c = mediciones[5]
    aux = 10 * 2 + (5 - 1)
    promedio = 0.0
    monto_total = 10 - 8

    % comentario con indentación que NO debe afectar

    print ("Iniciando proceso")

    {* comentario
    multilinea
    entre código *}
    print (mediciones)

    while c < aux

        % comentario dentro del bloque

        c = 7.7

        if c == mediciones[5]

            {* comentario dentro del if *}
            c = valor_mas_cercano(5, [1.0,2.0,5.4,8.0,10.0]) + valor_mas_cercano(7, [1.0,2.0,5.4,8.0,10.0])
            print ("Mitad de: camino ")
            print (c)
            break
        elif c > 8
            print (999)

        else

            % comentario antes de print

            print (0)

    % comentario entre bloques

    alt_while promedio < 100.0

        promedio = promedio + 10.5

        {* comentario multilínea dentro del while *}

        print (promedio)

    if ! (c == 0)

        % comentario final dentro del bloque

        print ("Fin del
        programa")